{
  "name": "tscs-subgrpah",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --access-token ''  --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ laplaceman/murmes-mumbai",
    "create-local": "graph create --node http://localhost:8020/ murmes-subgrpah",
    "remove-local": "graph remove --node http://localhost:8020/ murmes-subgrpah",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 murmes-subgrpah",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.35.0",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
